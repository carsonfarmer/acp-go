// This file is auto-generated by scripts/generate.js

import * as z from "zod/v4";

import type * as types from "./types.gen.js";
import * as validate from "./zod.gen.js";

function tagOf(value: unknown, key: string): unknown {
  return typeof value === "object" && value !== null
    ? (value as Record<string, unknown>)[key]
    : undefined;
}

const zGuardRequestPermissionSubjectToolCall =
  validate.zToolCallPermissionSubject.and(
    z.object({ type: z.literal("tool_call") }),
  );
const zGuardRequestPermissionSubjectCommand =
  validate.zCommandPermissionSubject.and(
    z.object({ type: z.literal("command") }),
  );
const zGuardToolCallContentContent = validate.zContent.and(
  z.object({ type: z.literal("content") }),
);
const zGuardToolCallContentDiff = validate.zDiff.and(
  z.object({ type: z.literal("diff") }),
);
const zGuardToolCallContentTerminal = validate.zTerminal.and(
  z.object({ type: z.literal("terminal") }),
);
const zGuardContentBlockText = validate.zTextContent.and(
  z.object({ type: z.literal("text") }),
);
const zGuardContentBlockImage = validate.zImageContent.and(
  z.object({ type: z.literal("image") }),
);
const zGuardContentBlockAudio = validate.zAudioContent.and(
  z.object({ type: z.literal("audio") }),
);
const zGuardContentBlockResourceLink = validate.zResourceLink.and(
  z.object({ type: z.literal("resource_link") }),
);
const zGuardContentBlockResource = validate.zEmbeddedResource.and(
  z.object({ type: z.literal("resource") }),
);
const zGuardDiffChangeAdd = validate.zDiffPathChange.and(
  z.object({ operation: z.literal("add") }),
);
const zGuardDiffChangeDelete = validate.zDiffPathChange.and(
  z.object({ operation: z.literal("delete") }),
);
const zGuardDiffChangeModify = validate.zDiffPathChange.and(
  z.object({ operation: z.literal("modify") }),
);
const zGuardDiffChangeMove = validate.zDiffPathPairChange.and(
  z.object({ operation: z.literal("move") }),
);
const zGuardDiffChangeCopy = validate.zDiffPathPairChange.and(
  z.object({ operation: z.literal("copy") }),
);
const zGuardCreateElicitationRequestForm = validate.zElicitationFormMode
  .and(z.object({ mode: z.literal("form") }))
  .and(z.object({ message: z.string() }));
const zGuardCreateElicitationRequestUrl = validate.zElicitationUrlMode
  .and(z.object({ mode: z.literal("url") }))
  .and(z.object({ message: z.string() }));
const zGuardCreateElicitationRequestCustom = z
  .union([validate.zElicitationSessionScope, validate.zElicitationRequestScope])
  .and(z.object({ message: z.string() }));
const zGuardElicitationPropertySchemaString =
  validate.zStringPropertySchema.and(z.object({ type: z.literal("string") }));
const zGuardElicitationPropertySchemaNumber =
  validate.zNumberPropertySchema.and(z.object({ type: z.literal("number") }));
const zGuardElicitationPropertySchemaInteger =
  validate.zIntegerPropertySchema.and(z.object({ type: z.literal("integer") }));
const zGuardElicitationPropertySchemaBoolean =
  validate.zBooleanPropertySchema.and(z.object({ type: z.literal("boolean") }));
const zGuardElicitationPropertySchemaArray =
  validate.zMultiSelectPropertySchema.and(
    z.object({ type: z.literal("array") }),
  );
const zGuardMultiSelectItemsString = validate.zStringMultiSelectItems.and(
  z.object({ type: z.literal("string") }),
);
const zGuardMultiSelectItemsTitled = validate.zTitledMultiSelectItems;
const zGuardAuthMethodTerminal = validate.zAuthMethodTerminal.and(
  z.object({ type: z.literal("terminal") }),
);
const zGuardAuthMethodAgent = validate.zAuthMethodAgent.and(
  z.object({ type: z.literal("agent") }),
);
const zGuardAuthMethodCustom = z.object({
  methodId: validate.zAuthMethodId,
  name: z.string(),
});
const zGuardSessionConfigOptionSelect = validate.zSessionConfigSelect
  .and(z.object({ type: z.literal("select") }))
  .and(z.object({ configId: validate.zSessionConfigId, name: z.string() }));
const zGuardSessionConfigOptionBoolean = validate.zSessionConfigBoolean
  .and(z.object({ type: z.literal("boolean") }))
  .and(z.object({ configId: validate.zSessionConfigId, name: z.string() }));
const zGuardSessionConfigOptionCustom = z.object({
  configId: validate.zSessionConfigId,
  name: z.string(),
});
const zGuardNesSuggestionEdit = validate.zNesEditSuggestion.and(
  z.object({ kind: z.literal("edit") }),
);
const zGuardNesSuggestionJump = validate.zNesJumpSuggestion.and(
  z.object({ kind: z.literal("jump") }),
);
const zGuardNesSuggestionRename = validate.zNesRenameSuggestion.and(
  z.object({ kind: z.literal("rename") }),
);
const zGuardNesSuggestionSearchAndReplace =
  validate.zNesSearchAndReplaceSuggestion.and(
    z.object({ kind: z.literal("searchAndReplace") }),
  );
const zGuardNesSuggestionCustom = z.object({
  suggestionId: validate.zNesSuggestionId,
});
const zGuardSessionUpdateUserMessageChunk = validate.zContentChunk.and(
  z.object({ sessionUpdate: z.literal("user_message_chunk") }),
);
const zGuardSessionUpdateUserMessage = validate.zUserMessage.and(
  z.object({ sessionUpdate: z.literal("user_message") }),
);
const zGuardSessionUpdateAgentMessageChunk = validate.zContentChunk.and(
  z.object({ sessionUpdate: z.literal("agent_message_chunk") }),
);
const zGuardSessionUpdateAgentMessage = validate.zAgentMessage.and(
  z.object({ sessionUpdate: z.literal("agent_message") }),
);
const zGuardSessionUpdateAgentThoughtChunk = validate.zContentChunk.and(
  z.object({ sessionUpdate: z.literal("agent_thought_chunk") }),
);
const zGuardSessionUpdateAgentThought = validate.zAgentThought.and(
  z.object({ sessionUpdate: z.literal("agent_thought") }),
);
const zGuardSessionUpdateStateUpdate = validate.zStateUpdate.and(
  z.object({ sessionUpdate: z.literal("state_update") }),
);
const zGuardSessionUpdateToolCallContentChunk =
  validate.zToolCallContentChunk.and(
    z.object({ sessionUpdate: z.literal("tool_call_content_chunk") }),
  );
const zGuardSessionUpdateToolCallUpdate = validate.zToolCallUpdate.and(
  z.object({ sessionUpdate: z.literal("tool_call_update") }),
);
const zGuardSessionUpdateTerminalUpdate = validate.zTerminalUpdate.and(
  z.object({ sessionUpdate: z.literal("terminal_update") }),
);
const zGuardSessionUpdateTerminalOutputChunk =
  validate.zTerminalOutputChunk.and(
    z.object({ sessionUpdate: z.literal("terminal_output_chunk") }),
  );
const zGuardSessionUpdatePlanUpdate = validate.zPlanUpdate.and(
  z.object({ sessionUpdate: z.literal("plan_update") }),
);
const zGuardSessionUpdatePlanRemoved = validate.zPlanRemoved.and(
  z.object({ sessionUpdate: z.literal("plan_removed") }),
);
const zGuardSessionUpdateAvailableCommandsUpdate =
  validate.zAvailableCommandsUpdate.and(
    z.object({ sessionUpdate: z.literal("available_commands_update") }),
  );
const zGuardSessionUpdateConfigOptionUpdate = validate.zConfigOptionUpdate.and(
  z.object({ sessionUpdate: z.literal("config_option_update") }),
);
const zGuardSessionUpdateSessionInfoUpdate = validate.zSessionInfoUpdate.and(
  z.object({ sessionUpdate: z.literal("session_info_update") }),
);
const zGuardSessionUpdateUsageUpdate = validate.zUsageUpdate.and(
  z.object({ sessionUpdate: z.literal("usage_update") }),
);
const zGuardSessionUpdateNotice = validate.zNotice.and(
  z.object({ sessionUpdate: z.literal("notice") }),
);
const zGuardSessionUpdateCompactionUpdate = validate.zCompactionUpdate.and(
  z.object({ sessionUpdate: z.literal("compaction_update") }),
);
const zGuardSessionUpdateCompactionSummaryChunk =
  validate.zCompactionSummaryChunk.and(
    z.object({ sessionUpdate: z.literal("compaction_summary_chunk") }),
  );
const zGuardStateUpdateRunning = validate.zRunningStateUpdate.and(
  z.object({ state: z.literal("running") }),
);
const zGuardStateUpdateIdle = validate.zIdleStateUpdate.and(
  z.object({ state: z.literal("idle") }),
);
const zGuardStateUpdateRequiresAction = validate.zRequiresActionStateUpdate.and(
  z.object({ state: z.literal("requires_action") }),
);
const zGuardPlanUpdateContentItems = validate.zPlanItems.and(
  z.object({ type: z.literal("items") }),
);
const zGuardPlanUpdateContentFile = validate.zPlanFile.and(
  z.object({ type: z.literal("file") }),
);
const zGuardPlanUpdateContentMarkdown = validate.zPlanMarkdown.and(
  z.object({ type: z.literal("markdown") }),
);
const zGuardPlanUpdateContentCustom = z.object({ planId: validate.zPlanId });
const zGuardAvailableCommandInputText = validate.zTextCommandInput.and(
  z.object({ type: z.literal("text") }),
);
const zGuardMcpServerHttp = validate.zMcpServerHttp.and(
  z.object({ type: z.literal("http") }),
);
const zGuardMcpServerAcp = validate.zMcpServerAcp.and(
  z.object({ type: z.literal("acp") }),
);
const zGuardMcpServerStdio = validate.zMcpServerStdio.and(
  z.object({ type: z.literal("stdio") }),
);
const zGuardReplayFromStart = validate.zReplayFromStart.and(
  z.object({ type: z.literal("start") }),
);
const zGuardSetSessionConfigOptionRequestId = z
  .object({ type: z.literal("id") })
  .and(z.object({ value: validate.zSessionConfigValueId }))
  .and(
    z.object({
      sessionId: validate.zSessionId,
      configId: validate.zSessionConfigId,
    }),
  );
const zGuardSetSessionConfigOptionRequestBoolean = z
  .object({ type: z.literal("boolean") })
  .and(z.object({ value: z.boolean() }))
  .and(
    z.object({
      sessionId: validate.zSessionId,
      configId: validate.zSessionConfigId,
    }),
  );
const zGuardSetSessionConfigOptionRequestCustom = z
  .object({ value: z.unknown() })
  .and(
    z.object({
      sessionId: validate.zSessionId,
      configId: validate.zSessionConfigId,
    }),
  );
const zGuardRequestPermissionOutcomeCancelled = z.object({
  outcome: z.literal("cancelled"),
});
const zGuardRequestPermissionOutcomeSelected =
  validate.zSelectedPermissionOutcome.and(
    z.object({ outcome: z.literal("selected") }),
  );
const zGuardCreateElicitationResponseAccept =
  validate.zElicitationAcceptAction.and(
    z.object({ action: z.literal("accept") }),
  );
const zGuardCreateElicitationResponseDecline = z.object({
  action: z.literal("decline"),
});
const zGuardCreateElicitationResponseCancel = z.object({
  action: z.literal("cancel"),
});

/**
 * The operation requiring permission.
 */
export type RequestPermissionSubject = types.RequestPermissionSubject;
/**
 * Validated type guards for `RequestPermissionSubject`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const RequestPermissionSubject = {
  /** Narrow to the `tool_call` variant, validating its payload. */
  isToolCall(
    value: types.RequestPermissionSubject,
  ): value is types.ToolCallPermissionSubject & { type: "tool_call" } {
    return (
      tagOf(value, "type") === "tool_call" &&
      zGuardRequestPermissionSubjectToolCall.safeParse(value).success
    );
  },

  /** Narrow to the `command` variant, validating its payload. */
  isCommand(
    value: types.RequestPermissionSubject,
  ): value is types.CommandPermissionSubject & { type: "command" } {
    return (
      tagOf(value, "type") === "command" &&
      zGuardRequestPermissionSubjectCommand.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.RequestPermissionSubject,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return typeof tag === "string" && !["command", "tool_call"].includes(tag);
  },
} as const;

/**
 * Content produced by a tool call.
 *
 * Tool calls can produce different types of content including standard
 * content blocks (text, images), file diffs, or display-only terminals.
 *
 * See protocol docs: [Content](https://agentclientprotocol.com/protocol/v2/draft/tool-calls#content)
 */
export type ToolCallContent = types.ToolCallContent;
/**
 * Validated type guards for `ToolCallContent`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const ToolCallContent = {
  /** Narrow to the `content` variant, validating its payload. */
  isContent(
    value: types.ToolCallContent,
  ): value is types.Content & { type: "content" } {
    return (
      tagOf(value, "type") === "content" &&
      zGuardToolCallContentContent.safeParse(value).success
    );
  },

  /** Narrow to the `diff` variant, validating its payload. */
  isDiff(value: types.ToolCallContent): value is types.Diff & { type: "diff" } {
    return (
      tagOf(value, "type") === "diff" &&
      zGuardToolCallContentDiff.safeParse(value).success
    );
  },

  /** Narrow to the `terminal` variant, validating its payload. */
  isTerminal(
    value: types.ToolCallContent,
  ): value is types.Terminal & { type: "terminal" } {
    return (
      tagOf(value, "type") === "terminal" &&
      zGuardToolCallContentTerminal.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.ToolCallContent,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" && !["content", "diff", "terminal"].includes(tag)
    );
  },
} as const;

/**
 * Content blocks represent displayable information in the Agent Client Protocol.
 *
 * They provide a structured way to handle various types of user-facing content—whether
 * it's text from language models, images for analysis, or embedded resources for context.
 *
 * Content blocks appear in:
 * - User prompts sent via `session/prompt`
 * - Language model output reported through `session/update` notifications as
 *   message updates or streamed chunks
 * - Progress updates and results from tool calls
 *
 * This structure is compatible with the Model Context Protocol (MCP), enabling
 * agents to seamlessly forward content from MCP tool outputs without transformation.
 *
 * See protocol docs: [Content](https://agentclientprotocol.com/protocol/v2/draft/content)
 */
export type ContentBlock = types.ContentBlock;
/**
 * Validated type guards for `ContentBlock`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const ContentBlock = {
  /** Narrow to the `text` variant, validating its payload. */
  isText(
    value: types.ContentBlock,
  ): value is types.TextContent & { type: "text" } {
    return (
      tagOf(value, "type") === "text" &&
      zGuardContentBlockText.safeParse(value).success
    );
  },

  /** Narrow to the `image` variant, validating its payload. */
  isImage(
    value: types.ContentBlock,
  ): value is types.ImageContent & { type: "image" } {
    return (
      tagOf(value, "type") === "image" &&
      zGuardContentBlockImage.safeParse(value).success
    );
  },

  /** Narrow to the `audio` variant, validating its payload. */
  isAudio(
    value: types.ContentBlock,
  ): value is types.AudioContent & { type: "audio" } {
    return (
      tagOf(value, "type") === "audio" &&
      zGuardContentBlockAudio.safeParse(value).success
    );
  },

  /** Narrow to the `resource_link` variant, validating its payload. */
  isResourceLink(
    value: types.ContentBlock,
  ): value is types.ResourceLink & { type: "resource_link" } {
    return (
      tagOf(value, "type") === "resource_link" &&
      zGuardContentBlockResourceLink.safeParse(value).success
    );
  },

  /** Narrow to the `resource` variant, validating its payload. */
  isResource(
    value: types.ContentBlock,
  ): value is types.EmbeddedResource & { type: "resource" } {
    return (
      tagOf(value, "type") === "resource" &&
      zGuardContentBlockResource.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.ContentBlock,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["audio", "image", "resource", "resource_link", "text"].includes(tag)
    );
  },
} as const;

/**
 * One file-level change described by a [`Diff`].
 *
 * Structured change metadata lets clients identify affected files and
 * operations without parsing the text patch.
 */
export type DiffChange = types.DiffChange;
/**
 * Validated type guards for `DiffChange`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const DiffChange = {
  /** Narrow to the `add` variant, validating its payload. */
  isAdd(
    value: types.DiffChange,
  ): value is (types.DiffPathChange & { operation: "add" }) &
    Pick<types.DiffChange, "fileType" | "mimeType" | "_meta"> {
    return (
      tagOf(value, "operation") === "add" &&
      zGuardDiffChangeAdd.safeParse(value).success
    );
  },

  /** Narrow to the `delete` variant, validating its payload. */
  isDelete(
    value: types.DiffChange,
  ): value is (types.DiffPathChange & { operation: "delete" }) &
    Pick<types.DiffChange, "fileType" | "mimeType" | "_meta"> {
    return (
      tagOf(value, "operation") === "delete" &&
      zGuardDiffChangeDelete.safeParse(value).success
    );
  },

  /** Narrow to the `modify` variant, validating its payload. */
  isModify(
    value: types.DiffChange,
  ): value is (types.DiffPathChange & { operation: "modify" }) &
    Pick<types.DiffChange, "fileType" | "mimeType" | "_meta"> {
    return (
      tagOf(value, "operation") === "modify" &&
      zGuardDiffChangeModify.safeParse(value).success
    );
  },

  /** Narrow to the `move` variant, validating its payload. */
  isMove(
    value: types.DiffChange,
  ): value is (types.DiffPathPairChange & { operation: "move" }) &
    Pick<types.DiffChange, "fileType" | "mimeType" | "_meta"> {
    return (
      tagOf(value, "operation") === "move" &&
      zGuardDiffChangeMove.safeParse(value).success
    );
  },

  /** Narrow to the `copy` variant, validating its payload. */
  isCopy(
    value: types.DiffChange,
  ): value is (types.DiffPathPairChange & { operation: "copy" }) &
    Pick<types.DiffChange, "fileType" | "mimeType" | "_meta"> {
    return (
      tagOf(value, "operation") === "copy" &&
      zGuardDiffChangeCopy.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `operation` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.DiffChange,
  ): value is { operation: string; [key: string]: unknown } & Pick<
    types.DiffChange,
    "fileType" | "mimeType" | "_meta"
  > {
    const tag = tagOf(value, "operation");
    return (
      typeof tag === "string" &&
      !["add", "copy", "delete", "modify", "move"].includes(tag)
    );
  },
} as const;

/**
 * Request from the agent to elicit structured user input.
 *
 * The agent sends this to the client to request information from the user,
 * either via a form or by directing them to a URL.
 * Elicitations are tied to a session (optionally a tool call) or a request.
 */
export type CreateElicitationRequest = types.CreateElicitationRequest;
/**
 * Validated type guards for `CreateElicitationRequest`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const CreateElicitationRequest = {
  /** Narrow to the `form` variant, validating its payload. */
  isForm(
    value: types.CreateElicitationRequest,
  ): value is (types.ElicitationFormMode & { mode: "form" }) &
    Pick<types.CreateElicitationRequest, "message" | "_meta"> {
    return (
      tagOf(value, "mode") === "form" &&
      zGuardCreateElicitationRequestForm.safeParse(value).success
    );
  },

  /** Narrow to the `url` variant, validating its payload. */
  isUrl(
    value: types.CreateElicitationRequest,
  ): value is (types.ElicitationUrlMode & { mode: "url" }) &
    Pick<types.CreateElicitationRequest, "message" | "_meta"> {
    return (
      tagOf(value, "mode") === "url" &&
      zGuardCreateElicitationRequestUrl.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `mode` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.CreateElicitationRequest,
  ): value is ((
    types.ElicitationSessionScope | types.ElicitationRequestScope
  ) & { mode: string; [key: string]: unknown }) &
    Pick<types.CreateElicitationRequest, "message" | "_meta"> {
    const tag = tagOf(value, "mode");
    return (
      typeof tag === "string" &&
      !["form", "url"].includes(tag) &&
      zGuardCreateElicitationRequestCustom.safeParse(value).success
    );
  },
} as const;

/**
 * Property schema for elicitation form fields.
 *
 * Each variant corresponds to a JSON Schema `"type"` value.
 * Single-select enums use the `String` variant with `enum` or `oneOf` set.
 * Multi-select enums use the `Array` variant.
 */
export type ElicitationPropertySchema = types.ElicitationPropertySchema;
/**
 * Validated type guards for `ElicitationPropertySchema`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const ElicitationPropertySchema = {
  /** Narrow to the `string` variant, validating its payload. */
  isString(
    value: types.ElicitationPropertySchema,
  ): value is types.StringPropertySchema & { type: "string" } {
    return (
      tagOf(value, "type") === "string" &&
      zGuardElicitationPropertySchemaString.safeParse(value).success
    );
  },

  /** Narrow to the `number` variant, validating its payload. */
  isNumber(
    value: types.ElicitationPropertySchema,
  ): value is types.NumberPropertySchema & { type: "number" } {
    return (
      tagOf(value, "type") === "number" &&
      zGuardElicitationPropertySchemaNumber.safeParse(value).success
    );
  },

  /** Narrow to the `integer` variant, validating its payload. */
  isInteger(
    value: types.ElicitationPropertySchema,
  ): value is types.IntegerPropertySchema & { type: "integer" } {
    return (
      tagOf(value, "type") === "integer" &&
      zGuardElicitationPropertySchemaInteger.safeParse(value).success
    );
  },

  /** Narrow to the `boolean` variant, validating its payload. */
  isBoolean(
    value: types.ElicitationPropertySchema,
  ): value is types.BooleanPropertySchema & { type: "boolean" } {
    return (
      tagOf(value, "type") === "boolean" &&
      zGuardElicitationPropertySchemaBoolean.safeParse(value).success
    );
  },

  /** Narrow to the `array` variant, validating its payload. */
  isArray(
    value: types.ElicitationPropertySchema,
  ): value is types.MultiSelectPropertySchema & { type: "array" } {
    return (
      tagOf(value, "type") === "array" &&
      zGuardElicitationPropertySchemaArray.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.ElicitationPropertySchema,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["array", "boolean", "integer", "number", "string"].includes(tag)
    );
  },
} as const;

/**
 * Items for a multi-select (array) property schema.
 */
export type MultiSelectItems = types.MultiSelectItems;
/**
 * Validated type guards for `MultiSelectItems`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const MultiSelectItems = {
  /** Narrow to the `string` variant, validating its payload. */
  isString(
    value: types.MultiSelectItems,
  ): value is types.StringMultiSelectItems & { type: "string" } {
    return (
      tagOf(value, "type") === "string" &&
      zGuardMultiSelectItemsString.safeParse(value).success
    );
  },

  /** Narrow to the `titled` variant, validating its payload. */
  isTitled(
    value: types.MultiSelectItems,
  ): value is types.TitledMultiSelectItems {
    return (
      tagOf(value, "type") === undefined &&
      zGuardMultiSelectItemsTitled.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.MultiSelectItems,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return typeof tag === "string" && !["string"].includes(tag);
  },
} as const;

/**
 * Describes an available authentication method.
 *
 * The `type` field acts as the discriminator in the serialized JSON form.
 */
export type AuthMethod = types.AuthMethod;
/**
 * Validated type guards for `AuthMethod`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const AuthMethod = {
  /** Narrow to the `terminal` variant, validating its payload. */
  isTerminal(
    value: types.AuthMethod,
  ): value is types.AuthMethodTerminal & { type: "terminal" } {
    return (
      tagOf(value, "type") === "terminal" &&
      zGuardAuthMethodTerminal.safeParse(value).success
    );
  },

  /** Narrow to the `agent` variant, validating its payload. */
  isAgent(
    value: types.AuthMethod,
  ): value is types.AuthMethodAgent & { type: "agent" } {
    return (
      tagOf(value, "type") === "agent" &&
      zGuardAuthMethodAgent.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(value: types.AuthMethod): value is {
    type: string;
    [key: string]: unknown;
  } & {
    methodId: types.AuthMethodId;
    name: string;
  } {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["agent", "terminal"].includes(tag) &&
      zGuardAuthMethodCustom.safeParse(value).success
    );
  },
} as const;

/**
 * A session configuration option selector and its current state.
 */
export type SessionConfigOption = types.SessionConfigOption;
/**
 * Validated type guards for `SessionConfigOption`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const SessionConfigOption = {
  /** Narrow to the `select` variant, validating its payload. */
  isSelect(
    value: types.SessionConfigOption,
  ): value is (types.SessionConfigSelect & { type: "select" }) &
    Pick<
      types.SessionConfigOption,
      "configId" | "name" | "description" | "category" | "_meta"
    > {
    return (
      tagOf(value, "type") === "select" &&
      zGuardSessionConfigOptionSelect.safeParse(value).success
    );
  },

  /** Narrow to the `boolean` variant, validating its payload. */
  isBoolean(
    value: types.SessionConfigOption,
  ): value is (types.SessionConfigBoolean & { type: "boolean" }) &
    Pick<
      types.SessionConfigOption,
      "configId" | "name" | "description" | "category" | "_meta"
    > {
    return (
      tagOf(value, "type") === "boolean" &&
      zGuardSessionConfigOptionBoolean.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.SessionConfigOption,
  ): value is { type: string; [key: string]: unknown } & Pick<
    types.SessionConfigOption,
    "configId" | "name" | "description" | "category" | "_meta"
  > {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["boolean", "select"].includes(tag) &&
      zGuardSessionConfigOptionCustom.safeParse(value).success
    );
  },
} as const;

/**
 * A suggestion returned by the agent.
 */
export type NesSuggestion = types.NesSuggestion;
/**
 * Validated type guards for `NesSuggestion`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const NesSuggestion = {
  /** Narrow to the `edit` variant, validating its payload. */
  isEdit(
    value: types.NesSuggestion,
  ): value is types.NesEditSuggestion & { kind: "edit" } {
    return (
      tagOf(value, "kind") === "edit" &&
      zGuardNesSuggestionEdit.safeParse(value).success
    );
  },

  /** Narrow to the `jump` variant, validating its payload. */
  isJump(
    value: types.NesSuggestion,
  ): value is types.NesJumpSuggestion & { kind: "jump" } {
    return (
      tagOf(value, "kind") === "jump" &&
      zGuardNesSuggestionJump.safeParse(value).success
    );
  },

  /** Narrow to the `rename` variant, validating its payload. */
  isRename(
    value: types.NesSuggestion,
  ): value is types.NesRenameSuggestion & { kind: "rename" } {
    return (
      tagOf(value, "kind") === "rename" &&
      zGuardNesSuggestionRename.safeParse(value).success
    );
  },

  /** Narrow to the `searchAndReplace` variant, validating its payload. */
  isSearchAndReplace(
    value: types.NesSuggestion,
  ): value is types.NesSearchAndReplaceSuggestion & {
    kind: "searchAndReplace";
  } {
    return (
      tagOf(value, "kind") === "searchAndReplace" &&
      zGuardNesSuggestionSearchAndReplace.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `kind` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(value: types.NesSuggestion): value is {
    kind: string;
    [key: string]: unknown;
  } & {
    suggestionId: types.NesSuggestionId;
  } {
    const tag = tagOf(value, "kind");
    return (
      typeof tag === "string" &&
      !["edit", "jump", "rename", "searchAndReplace"].includes(tag) &&
      zGuardNesSuggestionCustom.safeParse(value).success
    );
  },
} as const;

/**
 * Different types of updates that can be sent while a session exists.
 *
 * These updates report messages, progress, and other session activity.
 *
 * See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/v2/draft/prompt-lifecycle#3-agent-reports-output)
 */
export type SessionUpdate = types.SessionUpdate;
/**
 * Validated type guards for `SessionUpdate`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const SessionUpdate = {
  /** Narrow to the `user_message_chunk` variant, validating its payload. */
  isUserMessageChunk(
    value: types.SessionUpdate,
  ): value is types.ContentChunk & { sessionUpdate: "user_message_chunk" } {
    return (
      tagOf(value, "sessionUpdate") === "user_message_chunk" &&
      zGuardSessionUpdateUserMessageChunk.safeParse(value).success
    );
  },

  /** Narrow to the `user_message` variant, validating its payload. */
  isUserMessage(
    value: types.SessionUpdate,
  ): value is types.UserMessage & { sessionUpdate: "user_message" } {
    return (
      tagOf(value, "sessionUpdate") === "user_message" &&
      zGuardSessionUpdateUserMessage.safeParse(value).success
    );
  },

  /** Narrow to the `agent_message_chunk` variant, validating its payload. */
  isAgentMessageChunk(
    value: types.SessionUpdate,
  ): value is types.ContentChunk & { sessionUpdate: "agent_message_chunk" } {
    return (
      tagOf(value, "sessionUpdate") === "agent_message_chunk" &&
      zGuardSessionUpdateAgentMessageChunk.safeParse(value).success
    );
  },

  /** Narrow to the `agent_message` variant, validating its payload. */
  isAgentMessage(
    value: types.SessionUpdate,
  ): value is types.AgentMessage & { sessionUpdate: "agent_message" } {
    return (
      tagOf(value, "sessionUpdate") === "agent_message" &&
      zGuardSessionUpdateAgentMessage.safeParse(value).success
    );
  },

  /** Narrow to the `agent_thought_chunk` variant, validating its payload. */
  isAgentThoughtChunk(
    value: types.SessionUpdate,
  ): value is types.ContentChunk & { sessionUpdate: "agent_thought_chunk" } {
    return (
      tagOf(value, "sessionUpdate") === "agent_thought_chunk" &&
      zGuardSessionUpdateAgentThoughtChunk.safeParse(value).success
    );
  },

  /** Narrow to the `agent_thought` variant, validating its payload. */
  isAgentThought(
    value: types.SessionUpdate,
  ): value is types.AgentThought & { sessionUpdate: "agent_thought" } {
    return (
      tagOf(value, "sessionUpdate") === "agent_thought" &&
      zGuardSessionUpdateAgentThought.safeParse(value).success
    );
  },

  /** Narrow to the `state_update` variant, validating its payload. */
  isStateUpdate(
    value: types.SessionUpdate,
  ): value is types.StateUpdate & { sessionUpdate: "state_update" } {
    return (
      tagOf(value, "sessionUpdate") === "state_update" &&
      zGuardSessionUpdateStateUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `tool_call_content_chunk` variant, validating its payload. */
  isToolCallContentChunk(
    value: types.SessionUpdate,
  ): value is types.ToolCallContentChunk & {
    sessionUpdate: "tool_call_content_chunk";
  } {
    return (
      tagOf(value, "sessionUpdate") === "tool_call_content_chunk" &&
      zGuardSessionUpdateToolCallContentChunk.safeParse(value).success
    );
  },

  /** Narrow to the `tool_call_update` variant, validating its payload. */
  isToolCallUpdate(
    value: types.SessionUpdate,
  ): value is types.ToolCallUpdate & { sessionUpdate: "tool_call_update" } {
    return (
      tagOf(value, "sessionUpdate") === "tool_call_update" &&
      zGuardSessionUpdateToolCallUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `terminal_update` variant, validating its payload. */
  isTerminalUpdate(
    value: types.SessionUpdate,
  ): value is types.TerminalUpdate & { sessionUpdate: "terminal_update" } {
    return (
      tagOf(value, "sessionUpdate") === "terminal_update" &&
      zGuardSessionUpdateTerminalUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `terminal_output_chunk` variant, validating its payload. */
  isTerminalOutputChunk(
    value: types.SessionUpdate,
  ): value is types.TerminalOutputChunk & {
    sessionUpdate: "terminal_output_chunk";
  } {
    return (
      tagOf(value, "sessionUpdate") === "terminal_output_chunk" &&
      zGuardSessionUpdateTerminalOutputChunk.safeParse(value).success
    );
  },

  /** Narrow to the `plan_update` variant, validating its payload. */
  isPlanUpdate(
    value: types.SessionUpdate,
  ): value is types.PlanUpdate & { sessionUpdate: "plan_update" } {
    return (
      tagOf(value, "sessionUpdate") === "plan_update" &&
      zGuardSessionUpdatePlanUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `plan_removed` variant, validating its payload. */
  isPlanRemoved(
    value: types.SessionUpdate,
  ): value is types.PlanRemoved & { sessionUpdate: "plan_removed" } {
    return (
      tagOf(value, "sessionUpdate") === "plan_removed" &&
      zGuardSessionUpdatePlanRemoved.safeParse(value).success
    );
  },

  /** Narrow to the `available_commands_update` variant, validating its payload. */
  isAvailableCommandsUpdate(
    value: types.SessionUpdate,
  ): value is types.AvailableCommandsUpdate & {
    sessionUpdate: "available_commands_update";
  } {
    return (
      tagOf(value, "sessionUpdate") === "available_commands_update" &&
      zGuardSessionUpdateAvailableCommandsUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `config_option_update` variant, validating its payload. */
  isConfigOptionUpdate(
    value: types.SessionUpdate,
  ): value is types.ConfigOptionUpdate & {
    sessionUpdate: "config_option_update";
  } {
    return (
      tagOf(value, "sessionUpdate") === "config_option_update" &&
      zGuardSessionUpdateConfigOptionUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `session_info_update` variant, validating its payload. */
  isSessionInfoUpdate(
    value: types.SessionUpdate,
  ): value is types.SessionInfoUpdate & {
    sessionUpdate: "session_info_update";
  } {
    return (
      tagOf(value, "sessionUpdate") === "session_info_update" &&
      zGuardSessionUpdateSessionInfoUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `usage_update` variant, validating its payload. */
  isUsageUpdate(
    value: types.SessionUpdate,
  ): value is types.UsageUpdate & { sessionUpdate: "usage_update" } {
    return (
      tagOf(value, "sessionUpdate") === "usage_update" &&
      zGuardSessionUpdateUsageUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `notice` variant, validating its payload. */
  isNotice(
    value: types.SessionUpdate,
  ): value is types.Notice & { sessionUpdate: "notice" } {
    return (
      tagOf(value, "sessionUpdate") === "notice" &&
      zGuardSessionUpdateNotice.safeParse(value).success
    );
  },

  /** Narrow to the `compaction_update` variant, validating its payload. */
  isCompactionUpdate(
    value: types.SessionUpdate,
  ): value is types.CompactionUpdate & { sessionUpdate: "compaction_update" } {
    return (
      tagOf(value, "sessionUpdate") === "compaction_update" &&
      zGuardSessionUpdateCompactionUpdate.safeParse(value).success
    );
  },

  /** Narrow to the `compaction_summary_chunk` variant, validating its payload. */
  isCompactionSummaryChunk(
    value: types.SessionUpdate,
  ): value is types.CompactionSummaryChunk & {
    sessionUpdate: "compaction_summary_chunk";
  } {
    return (
      tagOf(value, "sessionUpdate") === "compaction_summary_chunk" &&
      zGuardSessionUpdateCompactionSummaryChunk.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `sessionUpdate` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.SessionUpdate,
  ): value is { sessionUpdate: string; [key: string]: unknown } {
    const tag = tagOf(value, "sessionUpdate");
    return (
      typeof tag === "string" &&
      ![
        "agent_message",
        "agent_message_chunk",
        "agent_thought",
        "agent_thought_chunk",
        "available_commands_update",
        "compaction_summary_chunk",
        "compaction_update",
        "config_option_update",
        "notice",
        "plan_removed",
        "plan_update",
        "session_info_update",
        "state_update",
        "terminal_output_chunk",
        "terminal_update",
        "tool_call_content_chunk",
        "tool_call_update",
        "usage_update",
        "user_message",
        "user_message_chunk",
      ].includes(tag)
    );
  },
} as const;

/**
 * The state of the agent's foreground work has changed.
 *
 * Background activity can continue and emit other `session/update` notifications
 * while `idle`. Those notifications do not change this state.
 */
export type StateUpdate = types.StateUpdate;
/**
 * Validated type guards for `StateUpdate`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const StateUpdate = {
  /** Narrow to the `running` variant, validating its payload. */
  isRunning(
    value: types.StateUpdate,
  ): value is types.RunningStateUpdate & { state: "running" } {
    return (
      tagOf(value, "state") === "running" &&
      zGuardStateUpdateRunning.safeParse(value).success
    );
  },

  /** Narrow to the `idle` variant, validating its payload. */
  isIdle(
    value: types.StateUpdate,
  ): value is types.IdleStateUpdate & { state: "idle" } {
    return (
      tagOf(value, "state") === "idle" &&
      zGuardStateUpdateIdle.safeParse(value).success
    );
  },

  /** Narrow to the `requires_action` variant, validating its payload. */
  isRequiresAction(
    value: types.StateUpdate,
  ): value is types.RequiresActionStateUpdate & { state: "requires_action" } {
    return (
      tagOf(value, "state") === "requires_action" &&
      zGuardStateUpdateRequiresAction.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `state` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.StateUpdate,
  ): value is { state: string; [key: string]: unknown } {
    const tag = tagOf(value, "state");
    return (
      typeof tag === "string" &&
      !["idle", "requires_action", "running"].includes(tag)
    );
  },
} as const;

/**
 * Updated content for a plan.
 */
export type PlanUpdateContent = types.PlanUpdateContent;
/**
 * Validated type guards for `PlanUpdateContent`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const PlanUpdateContent = {
  /** Narrow to the `items` variant, validating its payload. */
  isItems(
    value: types.PlanUpdateContent,
  ): value is types.PlanItems & { type: "items" } {
    return (
      tagOf(value, "type") === "items" &&
      zGuardPlanUpdateContentItems.safeParse(value).success
    );
  },

  /** Narrow to the `file` variant, validating its payload. */
  isFile(
    value: types.PlanUpdateContent,
  ): value is types.PlanFile & { type: "file" } {
    return (
      tagOf(value, "type") === "file" &&
      zGuardPlanUpdateContentFile.safeParse(value).success
    );
  },

  /** Narrow to the `markdown` variant, validating its payload. */
  isMarkdown(
    value: types.PlanUpdateContent,
  ): value is types.PlanMarkdown & { type: "markdown" } {
    return (
      tagOf(value, "type") === "markdown" &&
      zGuardPlanUpdateContentMarkdown.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(value: types.PlanUpdateContent): value is {
    type: string;
    [key: string]: unknown;
  } & {
    planId: types.PlanId;
  } {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["file", "items", "markdown"].includes(tag) &&
      zGuardPlanUpdateContentCustom.safeParse(value).success
    );
  },
} as const;

/**
 * The input specification for a command.
 */
export type AvailableCommandInput = types.AvailableCommandInput;
/**
 * Validated type guards for `AvailableCommandInput`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const AvailableCommandInput = {
  /** Narrow to the `text` variant, validating its payload. */
  isText(
    value: types.AvailableCommandInput,
  ): value is types.TextCommandInput & { type: "text" } {
    return (
      tagOf(value, "type") === "text" &&
      zGuardAvailableCommandInputText.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.AvailableCommandInput,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return typeof tag === "string" && !["text"].includes(tag);
  },
} as const;

/**
 * Configuration for connecting to an MCP (Model Context Protocol) server.
 *
 * MCP servers provide tools and context that the agent can use when
 * processing prompts.
 *
 * See protocol docs: [MCP Servers](https://agentclientprotocol.com/protocol/v2/draft/session-setup#mcp-servers)
 */
export type McpServer = types.McpServer;
/**
 * Validated type guards for `McpServer`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const McpServer = {
  /** Narrow to the `http` variant, validating its payload. */
  isHttp(
    value: types.McpServer,
  ): value is types.McpServerHttp & { type: "http" } {
    return (
      tagOf(value, "type") === "http" &&
      zGuardMcpServerHttp.safeParse(value).success
    );
  },

  /** Narrow to the `acp` variant, validating its payload. */
  isAcp(value: types.McpServer): value is types.McpServerAcp & { type: "acp" } {
    return (
      tagOf(value, "type") === "acp" &&
      zGuardMcpServerAcp.safeParse(value).success
    );
  },

  /** Narrow to the `stdio` variant, validating its payload. */
  isStdio(
    value: types.McpServer,
  ): value is types.McpServerStdio & { type: "stdio" } {
    return (
      tagOf(value, "type") === "stdio" &&
      zGuardMcpServerStdio.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.McpServer,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return typeof tag === "string" && !["acp", "http", "stdio"].includes(tag);
  },
} as const;

/**
 * Inclusive cursor describing where replayed session history should begin.
 *
 * Replay includes the position identified by the cursor.
 */
export type ReplayFrom = types.ReplayFrom;
/**
 * Validated type guards for `ReplayFrom`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const ReplayFrom = {
  /** Narrow to the `start` variant, validating its payload. */
  isStart(
    value: types.ReplayFrom,
  ): value is types.ReplayFromStart & { type: "start" } {
    return (
      tagOf(value, "type") === "start" &&
      zGuardReplayFromStart.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.ReplayFrom,
  ): value is { type: string; [key: string]: unknown } {
    const tag = tagOf(value, "type");
    return typeof tag === "string" && !["start"].includes(tag);
  },
} as const;

/**
 * Request parameters for setting a session configuration option.
 */
export type SetSessionConfigOptionRequest = types.SetSessionConfigOptionRequest;
/**
 * Validated type guards for `SetSessionConfigOptionRequest`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const SetSessionConfigOptionRequest = {
  /** Narrow to the `id` variant, validating its payload. */
  isId(
    value: types.SetSessionConfigOptionRequest,
  ): value is ({ type: "id" } & { value: types.SessionConfigValueId }) &
    Pick<
      types.SetSessionConfigOptionRequest,
      "sessionId" | "configId" | "_meta"
    > {
    return (
      tagOf(value, "type") === "id" &&
      zGuardSetSessionConfigOptionRequestId.safeParse(value).success
    );
  },

  /** Narrow to the `boolean` variant, validating its payload. */
  isBoolean(
    value: types.SetSessionConfigOptionRequest,
  ): value is ({ type: "boolean" } & { value: boolean }) &
    Pick<
      types.SetSessionConfigOptionRequest,
      "sessionId" | "configId" | "_meta"
    > {
    return (
      tagOf(value, "type") === "boolean" &&
      zGuardSetSessionConfigOptionRequestBoolean.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `type` tag matches no known variant, with a valid payload.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.SetSessionConfigOptionRequest,
  ): value is ({ type: string; [key: string]: unknown } & { value: unknown }) &
    Pick<
      types.SetSessionConfigOptionRequest,
      "sessionId" | "configId" | "_meta"
    > {
    const tag = tagOf(value, "type");
    return (
      typeof tag === "string" &&
      !["boolean", "id"].includes(tag) &&
      zGuardSetSessionConfigOptionRequestCustom.safeParse(value).success
    );
  },
} as const;

/**
 * The outcome of a permission request.
 */
export type RequestPermissionOutcome = types.RequestPermissionOutcome;
/**
 * Validated type guards for `RequestPermissionOutcome`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const RequestPermissionOutcome = {
  /** Narrow to the `cancelled` variant, validating its payload. */
  isCancelled(
    value: types.RequestPermissionOutcome,
  ): value is { outcome: "cancelled" } {
    return (
      tagOf(value, "outcome") === "cancelled" &&
      zGuardRequestPermissionOutcomeCancelled.safeParse(value).success
    );
  },

  /** Narrow to the `selected` variant, validating its payload. */
  isSelected(
    value: types.RequestPermissionOutcome,
  ): value is types.SelectedPermissionOutcome & { outcome: "selected" } {
    return (
      tagOf(value, "outcome") === "selected" &&
      zGuardRequestPermissionOutcomeSelected.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `outcome` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.RequestPermissionOutcome,
  ): value is { outcome: string; [key: string]: unknown } {
    const tag = tagOf(value, "outcome");
    return typeof tag === "string" && !["cancelled", "selected"].includes(tag);
  },
} as const;

/**
 * Response from the client to an elicitation request.
 */
export type CreateElicitationResponse = types.CreateElicitationResponse;
/**
 * Validated type guards for `CreateElicitationResponse`'s known variants.
 *
 * Each guard validates the variant's payload, not just its discriminant
 * tag: a malformed known variant (right tag, wrong payload) matches no
 * guard — mirroring wire validation, which rejects such values instead
 * of classifying them as custom.
 *
 * Guards check the value as given: fields that wire deserialization
 * salvages to a default (e.g. a malformed `_meta`) are only normalized
 * by parsing, and for ambiguous raw shapes (a known tag combined with
 * another variant's payload) guards are conservative where wire parsing
 * may still accept the value — narrow wire-parsed values when exact
 * parity matters.
 */
export const CreateElicitationResponse = {
  /** Narrow to the `accept` variant, validating its payload. */
  isAccept(
    value: types.CreateElicitationResponse,
  ): value is (types.ElicitationAcceptAction & { action: "accept" }) &
    Pick<types.CreateElicitationResponse, "_meta"> {
    return (
      tagOf(value, "action") === "accept" &&
      zGuardCreateElicitationResponseAccept.safeParse(value).success
    );
  },

  /** Narrow to the `decline` variant, validating its payload. */
  isDecline(
    value: types.CreateElicitationResponse,
  ): value is { action: "decline" } & Pick<
    types.CreateElicitationResponse,
    "_meta"
  > {
    return (
      tagOf(value, "action") === "decline" &&
      zGuardCreateElicitationResponseDecline.safeParse(value).success
    );
  },

  /** Narrow to the `cancel` variant, validating its payload. */
  isCancel(
    value: types.CreateElicitationResponse,
  ): value is { action: "cancel" } & Pick<
    types.CreateElicitationResponse,
    "_meta"
  > {
    return (
      tagOf(value, "action") === "cancel" &&
      zGuardCreateElicitationResponseCancel.safeParse(value).success
    );
  },

  /**
   * Narrow to a custom or future variant: the `action` tag matches no known variant.
   *
   * TypeScript keeps the known variants in the narrowed union (they are
   * structural subtypes of the catch-all), so read vendor payload keys
   * via a widening cast: `(value as Record<string, unknown>).someKey`.
   */
  isCustom(
    value: types.CreateElicitationResponse,
  ): value is { action: string; [key: string]: unknown } & Pick<
    types.CreateElicitationResponse,
    "_meta"
  > {
    const tag = tagOf(value, "action");
    return (
      typeof tag === "string" && !["accept", "cancel", "decline"].includes(tag)
    );
  },
} as const;
